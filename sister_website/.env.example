# Flask
SECRET_KEY=your-secret-key-here

# Database
DATABASE_URL=sqlite:///submissions.db

# ForwardEmail Configuration
# For local development, this file (.env) will be loaded automatically.
# For production, create this file at the path specified by the DOTENV_PATH environment variable
# (e.g., /var/www/.sister.env as per DEPLOYMENT.MD) and ensure DOTENV_PATH is set for the application process.

# ForwardEmail API Settings
FORWARD_EMAIL_API_KEY=your-forwardemail-api-key
FORWARD_EMAIL_FROM_EMAIL=your-verified-email@yourdomain.com
FORWARD_EMAIL_REPLY_TO=your-reply-to-email@yourdomain.com

# Application Settings
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=33554432  # 32MB in bytes

# Webhook Configuration (if using email replies)
# The URL where ForwardEmail will send email reply notifications
# WEBHOOK_BASE_URL=https://yourdomain.com
